/* MALVA não usa Tailwind. Este arquivo existe para ancorar a busca do
   PostCSS aqui e impedir que a configuração de um projeto vizinho,
   na pasta acima, seja aplicada ao build deste site. */
export default { plugins: {} }
